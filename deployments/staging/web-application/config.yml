release_name: "web-app"
chart: "../../../charts/nginx_basic"
namespace: "basic"

helm_values:
  service:
    type: ClusterIP
  ingress:
    enabled: true
    className: "nginx"
    hosts:
      - host: s-myapp.local
        paths:
          - path: /
            pathType: Prefix
